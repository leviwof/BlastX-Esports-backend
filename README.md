# BlastX Esports Backend

NestJS backend for BlastX Esports.

## Run locally

```bash
npm install
npm run start:dev
```

Then open [http://localhost:3000](http://localhost:3000). The API returns:

```text
Hello BlastX Esports!
```
